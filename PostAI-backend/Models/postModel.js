class Post {
    constructor(id, content, userId, typeNetwork, topic) {
        (this.id = id),
        (this.content = content),
        (this.userId = userId),
        (this.typeNetwork = typeNetwork),
        (this.topic = topic);
    }
}
  
export default Post;